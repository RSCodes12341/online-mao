# Copy this file to .env.local for local dev overrides.
# In production (Vercel/Netlify) set these as environment variables in the dashboard.

# Your deployed backend URL (no trailing slash, no /ws suffix)
# Example: https://online-mau-backend.onrender.com
VITE_API_URL=http://localhost:8000
